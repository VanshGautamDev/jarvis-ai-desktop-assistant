# JARVIS AI Desktop Assistant - Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
speechrecognition==3.10.0
pyttsx3==2.90
pyaudio==0.2.11
pyautogui==0.9.54

# System monitoring and control
psutil==5.9.5
subprocess32==3.5.4; python_version < '3.0'

# AI Integration
anthropic>=0.3.0
openai>=0.27.0
requests==2.31.0

# GUI (comes with Python by default, but listed for completeness)
# tkinter - included with Python

# File and web operations
pathlib2==2.3.7; python_version < '3.4'
webbrowser  # Built-in module

# Audio/Media handling
pygame==2.5.2
mutagen==1.47.0

# Optional: For better audio handling
sounddevice==0.4.6
soundfile==0.12.1

# Optional: For YouTube integration (requires separate installation)
# youtube-dl==2021.12.17
# yt-dlp==2023.7.6

# Optional: For advanced voice recognition
# vosk==0.3.45
# torch==2.0.1
# torchaudio==2.0.2

# Development and logging
logging  # Built-in module
threading  # Built-in module
queue  # Built-in module

# Note: Some packages may require additional system dependencies:
# 
# For Windows:
# - Microsoft Visual C++ 14.0 or greater (for some audio packages)
# - Windows SDK (for system control features)
#
# For macOS:
# - Xcode command line tools
# - PortAudio (for PyAudio): brew install portaudio
#
# For Linux (Ubuntu/Debian):
# - sudo apt-get install python3-dev python3-pip
# - sudo apt-get install portaudio19-dev
# - sudo apt-get install espeak espeak-data libespeak1 libespeak-dev
# - sudo apt-get install ffmpeg (for media handling)
#
# PyAudio installation troubleshooting:
# Windows: pip install pipwin && pipwin install pyaudio
# macOS: brew install portaudio && pip install pyaudio
# Linux: sudo apt-get install portaudio19-dev && pip install pyaudio